import SwiftUI

struct ReportView: View {
    var body: some View {
        Text("Report Page")
            .font(.largeTitle)
            .fontWeight(.bold)
            .foregroundColor(.orange)
    }
}
